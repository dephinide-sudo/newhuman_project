# PRD · 갈까말까 AI

**작성 단계**: 도전학기 신규 서비스 기획 · MVP 스펙
**한 줄 정의**: 애매한 약속의 참석/거절을 관계·리소스·컨디션 기반으로 AI가 대신 판단해주고, 거절이 필요할 땐 문구까지 함께 제안하는 서비스

---

## 1. 문제 정의

현대인은 친구·동아리·학교·직장 등 다양한 관계에서 수많은 약속에 초대받지만 모든 약속에 참석할 수는 없다. 특히 **친밀도나 중요도가 애매한 약속**일수록 참석 여부를 정하는 데 시간과 심리적 에너지가 소모되고, 거절해야 하는 상황에서는 상대방과의 관계가 나빠질까 걱정되어 거절 자체를 부담스러워한다.

- **판단 어려움**: 친한 친구부터 애매한 지인까지, 어떤 약속에 우선순위를 둬야 할지 판단하기 어렵다.
- **거절 부담**: 가고 싶지 않아도 상대방의 기분을 고려하다 쉽게 거절하지 못한다.
- **리소스 부담**: 이동시간·비용을 써야 하는데, 실제로 그만한 가치가 있는지 판단이 어렵다.
- **관계 고민**: 참석하지 않았을 때 관계에 미칠 영향까지 생각하다 결정이 더 어려워진다.

> **문제 정의 통합 메모**: 초안 단계에서는 문제를 4개 페인포인트로 나열했고, 팀 논의 단계에서는 "선택에 대한 확신 부족"으로 좁혀졌다. 본 PRD는 두 관점을 통합해, **판단 자체(합리적 근거)의 어려움**과 **거절이라는 행동(심리적 부담)의 어려움**을 모두 다루는 구조로 설계한다. 결과 화면이 "판단 근거"와 "감정적 지지 + 거절 문구"를 함께 담는 이유가 여기에 있다.

## 2. 타겟

| 구분 | 내용 |
|---|---|
| 핵심 타겟 | 약속이 잦은 대학생·직장인, 20대 초중반 |
| 성향 | 자신의 선택에 완전한 확신이 없는 사람, 대인관계에 부담을 느끼는 사람 |
| 사용 맥락 | 매일 쓰는 도구가 아니라, **애매한 약속이 생겼을 때** 저빈도·고집중으로 찾는 서비스 |

## 3. 목표 (Goals)

1. 애매한 약속에 대한 **참석/거절 판단 시간**을 체감상 단축한다.
2. 판단 근거(관계·리소스·컨디션)를 구조화해 제시함으로써 **더 합리적인 결정**을 돕는다.
3. 거절이 필요한 상황에서 **바로 쓸 수 있는 문구**를 제공해 심리적 부담을 낮춘다.
4. MVP를 통해 "판단 완료 → 행동 전환(문구 복사 / 피드백)"으로 이어지는 **핵심 사용 루프**를 검증한다.

## 4. 하지 않는 것 (Non-goals)

| Non-goal | 이유 |
|---|---|
| 캘린더/메신저 자동 연동, 선제적 알림 | MVP 리소스 범위를 초과, v2 이후 검토 |
| 대화형(챗봇) 카운슬링 플로우 | 입력 마찰이 커 완료율을 떨어뜨릴 위험, 원샷 폼으로 우선 검증 |
| 자연어 프리폼 입력 ("이런 상황인데 어떡하지" 형태) | 파싱 실패 시 재질문 로직이 필요해 MVP엔 과함 |
| 참석 판단의 법적/의학적 책임 있는 조언 | 서비스 목적과 무관, 참고용 판단 도구로 포지셔닝 |
| 회원가입·계정 시스템 | 1회성 사용 흐름 검증이 우선, 세션 단위로 충분 |

## 5. 핵심 플로우

```
대문(서비스 소개 + 시작하기) → 정보 입력 → AI 판단 처리 → 결과카드(판단+확신도+근거+공감 코멘트)
                                                                  ├─ 참석 추천 시 → 다음 액션 제안
                                                                  └─ 거절 추천 시 → 거절 문구 자동 생성
                                                                                         ↓
                                                                     자율 선택 안내(마무리, 단일 스크롤로 결과에 포함)
```

- 대문 화면은 최초 진입 시에만 노출되며, 결과 화면의 "다른 약속으로 다시 해보기"는 대문을 거치지 않고 정보 입력으로 바로 돌아간다. 재사용 시 소개를 반복해서 볼 필요가 없다고 판단했기 때문이다.

- 결과 화면은 판단 → 근거 → (조건부) 거절 문구 → 마무리 안내까지 **하나의 스크롤**로 이어진다. 마무리 안내를 결과카드에서 분리해 톤을 "당신 최종 결정은 몫이에요"로 다듬은 이유는, 핵심 CTA(문구 복사)가 먼저 완결되게 하고 안심 메시지가 CTA 동선을 방해하지 않게 하기 위함이다.

## 6. 유저 스토리

- 확신이 없는 대학생으로서, 애매한 동아리 선배의 약속 정보를 입력하면 참석 여부에 대한 근거 있는 판단을 받고 싶다. 스스로 결정하는 부담을 줄이기 위해서다.
- 거절이 필요한 직장인으로서, AI가 거절을 추천했을 때 바로 보낼 수 있는 문구를 톤별로 받고 싶다. 문구를 직접 고민하는 시간을 줄이기 위해서다.
- 관계에 부담을 느끼는 사용자로서, 결과를 받은 뒤에도 "AI 말과 다르게 행동해도 괜찮다"는 안내를 보고 싶다. 판단에 지나치게 의존하지 않기 위해서다.
- 서비스 운영자로서, 사용자가 판단을 완료하고 문구를 복사하거나 피드백을 남기는 비율을 보고 싶다. MVP 가설(판단이 실제로 도움이 되는가)을 검증하기 위해서다.

## 7. 요구사항

### P0 (Must-have)
| 항목 | 설명 | 인수 조건 |
|---|---|---|
| 대문 화면 | 서비스 소개, 3단계 작동 방식 안내, "시작하기" CTA | Given 최초 진입 시 When 페이지가 로드되면 Then 대문 화면이 먼저 표시되고, "시작하기"를 누르면 정보 입력으로 이동한다 |
| 정보 입력 폼 | 관계, 다음 기회 여부, 소요 시간, 예상 비용, 컨디션 5개 항목 | Given 필수 항목을 모두 선택/입력했을 때 When "이제 판단해줘"를 누르면 Then 로딩 화면으로 전환된다 |
| 판단 로직 | 입력값 기반으로 참석/거절 + 확신도(%) 산출 | 동일 입력값에 대해 동일한 결과가 재현된다 |
| 결과카드 | 판단 결론, 확신도 바, 근거 최대 3개, 공감 코멘트를 한 화면 스크롤로 표시 | 근거는 입력값에서 실제로 가중치가 높은 항목 기준으로 노출된다 |
| 거절 문구 생성 | 거절 판단일 때만 노출, 톤 2종(정중하게/편하게) 전환 가능, 원탭 복사 | Given 결과가 "거절"일 때 When 문구 복사 버튼을 누르면 Then 클립보드에 문구가 복사되고 버튼 상태가 "복사했어요"로 바뀐다 |
| 자율성 안내 | 결과 화면 하단에 "최종 결정은 당신 몫" 메시지 노출 (핵심 CTA 이후 위치) | 문구 복사 UI보다 스크롤상 아래에 위치한다 |
| 피드백 CTA | "도움됐어요 / 아쉬워요" 2개 버튼 | 클릭 시 선택 상태가 표시되고 재클릭이 방지된다 |

### P1 (Nice-to-have)
| 항목 | 설명 |
|---|---|
| 참석 추천 시 다음 액션 제안 | 예: "대신 다음 주 커피 약속 제안해보기" 문구 생성 |
| 결과 공유하기 | 결과카드를 이미지/링크로 공유 (SNS 유입 채널 확보) |
| 입력 이력 기반 재사용 | 최근 입력값 불러오기 |

### P2 (Future considerations)
| 항목 | 설명 |
|---|---|
| 대화형 카운슬링 모드 | "확신 부족" 문제에 더 깊이 대응하는 v2 플로우 |
| 자연어 프리폼 입력 | 마찰을 줄이는 입력 방식, 파싱 신뢰도 확보 후 도입 |
| 캘린더 연동 | 다가오는 약속을 자동 인식해 선제적으로 판단 제안 |

## 8. 성공 지표

### Leading (출시 직후 관찰)
| 지표 | 정의 | 잠정 타겟 |
|---|---|---|
| 판단 완료율 | 입력 시작 대비 결과 화면 도달 비율 | 60%+ |
| 거절 문구 복사율 | 거절 결과 사용자 중 복사 버튼 클릭 비율 (핵심 CTA) | 40%+ |
| 피드백 응답률 | 결과 확인 후 피드백 버튼 클릭 비율 | 30%+ |

### Lagging (누적 관찰)
| 지표 | 정의 |
|---|---|
| 재방문율 | 서비스 특성상 저빈도이므로 참고 지표로만 활용 |
| "도움됐어요" 비율 | 판단의 체감 유용성 검증 |

측정 도구는 Google Analytics(이벤트: `form_submit`, `result_view`, `copy_message`, `feedback_click`) + Supabase(입력값·결과·피드백 로그 적재)로 계획한다.

## 9. 기술/배포 개요

- **프론트엔드**: 정적 HTML/CSS/JS (`index.html`), Git 저장소 → Vercel 배포
- **데이터**: Supabase(입력 로그, 결과 로그, 피드백 로그)
- **분석**: Google Analytics 이벤트 트래킹
- **SEO**: 메타 태그, 서비스 설명 등록
- **AI 판단 로직**: MVP는 클라이언트 사이드 규칙 기반 스코어링으로 프로토타입 검증 후, 실제 LLM API 연동으로 대체 검토

## 10. 열린 질문

| 질문 | 담당 |
|---|---|
| 판단 로직을 규칙 기반으로 유지할지, LLM API 호출로 전환할지 | 엔지니어링 |
| 거절 문구 톤을 2종에서 늘릴지 (예: 업무용 격식체 추가) | 기획/디자인 |
| "이번뿐이에요" 같은 이지선다 입력이 실제 사용자 상황을 충분히 반영하는지 | 리서치(인터뷰) |
| 피드백 데이터를 판단 로직 개선에 어떻게 반영할지 | 데이터 |

## 11. 타임라인 고려사항

- MVP(P0) 우선 구현 → 인터뷰 진행 후 문구/입력 항목 보정
- 발표 전까지 GA/Supabase 연동 및 CTA 지표 수집 완료
- v2 검토 항목(P2)은 발표 시 "기대효과 및 한계점" 섹션에서 로드맵으로만 언급
