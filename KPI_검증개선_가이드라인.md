# 갈까말까 AI — KPI 및 검증·개선 가이드라인

> 이 문서는 [PRD_갈까말까AI.md](PRD_갈까말까AI.md)의 성공지표(8절)를 근거로, 발표/평가에서 요구하는
> "MVP 완성도"가 아닌 **검증·개선 과정**을 실제로 운영하기 위한 실행 가이드다.

## 1. 메인 KPI: 행동 전환율

```
행동 전환율 = (copy_message 또는 feedback_click이 발생한 세션 수) ÷ (result_view가 발생한 세션 수)
```

### 왜 이걸 메인(North Star)으로 잡는가
- PRD 목표 4번과 정확히 일치: *"판단 완료 → 행동 전환(문구 복사 / 피드백)으로 이어지는 핵심 사용 루프를 검증한다."*
- 설문 같은 자기보고가 아니라 실제 클릭 데이터라서, MVP의 핵심 가설 — **"판단이 실제로 도움이 되는가"** — 을 가장 직접적으로 검증한다.
- PRD가 나열한 leading 지표 3종(판단완료율/복사율/피드백응답률)을 하나의 스토리 지표로 통합한다.

## 2. 보조 진단 지표

North Star가 흔들릴 때 원인을 찾기 위한 분해 지표들.

| 지표 | 정의 | 목표(PRD) | 진단 대상 |
|---|---|---|---|
| 판단완료율 | form_submit → result_view | 60%+ | 입력 단계 이탈 |
| 거절문구 복사율 | decline 결과 중 copy_message | 40%+ | 결과가 실질적으로 쓸모 있었는지 |
| 피드백 응답률 | result_view → feedback_click | 30%+ | 참여도 |
| "도움됐어요" 비율 | good / 전체 피드백 | 참고용 | 품질(perceived usefulness) 신호 |
| score/top_factors vs rating 상관관계 | `judgments` 테이블 조인 | 참고용 | 판단 로직 자체의 타당성 |

측정 도구: Google Analytics(이벤트: `form_submit`, `result_view`, `copy_message`, `feedback_click`) + Supabase(`funnel_events`, `judgments` 테이블).

## 3. 검증·개선 사이클 (Build → Measure → Learn)

1. **베이스라인 수집**
   실사용 트래픽으로 최소 30~50세션을 모은 뒤, `supabase/schema.sql`의 퍼널 드롭오프 쿼리로
   `form_submit → result_view → copy_message → feedback_click` 4단계 스냅샷을 뜬다.

2. **병목 진단**
   어느 단계 이탈이 가장 큰지 확인하고, 그에 맞는 가설을 세운다.
   - 판단완료율이 낮다 → 입력 단계 마찰 가설 (PRD 열린 질문: "이번뿐이에요" 이진 입력이 실제 상황을 충분히 반영하는가) → 소규모 인터뷰로 확인
   - result_view → 행동전환이 낮다 → 결과 화면 카피/CTA/신뢰도 표현 방식 가설
   - "아쉬워요" 비율이 높다 → `judgments`와 `feedback_click`을 relation/nextchance 기준으로 조인해, 어떤 입력 조합에서 부정 피드백이 몰리는지 분석 → 스코어링 가중치 조정 가설

3. **단일 변수 변경**
   가설 하나당 변경 하나만 적용한다 (문구·가중치·UI를 동시에 바꾸지 않는다 — 효과 귀속이 불가능해지기 때문).

4. **재측정 & 비교**
   같은 KPI로 변경 전/후를 비교한다.

5. **기록**
   라운드별 `가설 → 변경 → 결과`를 이 문서 4절(로그)에 계속 추가한다. 이 기록 자체가 "검증 개선 과정" 증거가 된다.

## 4. 라운드 로그

> 라운드가 진행될 때마다 아래에 이어서 기록한다.

| 라운드 | 날짜 | 가설 | 변경 내용 | 측정 결과 | 결론 |
|---|---|---|---|---|---|
| 0 (베이스라인) | | | GA4/Supabase 이벤트 트래킹 정상화 | 대기 중 | |

## 5. 발표 프레이밍

> "우리는 처음에 X라고 가정했다 → 실제 행동전환율 Y%로 확인해보니 Z 지점에서 이탈이 컸다 →
> 그래서 A를 바꿨다 → 재측정 결과 B로 개선됐다."

이 사이클을 최소 1바퀴 실제로 도는 것이, MVP를 더 완성도 있게 다듬는 것보다 이 과제의 평가축에서
더 중요하다.
